
################
### MONITORS ###
################

#monitor=HDMI-A-1,1920x1080@60,auto, 1
#monitor=HDMI-A-1,1920x1080@60, 0x0, 1
#misc{
 #   vrr = 1
#}

monitor=HDMI-A-1,1920x1080@60,0x0,1

misc {
    vrr=true
}

###################
### MY PROGRAMS ###
###################

# Set programs that you use
$terminal = kitty
$fileManager = dolphin
$menu = "$HOME/rofi-pessoal/launcher.sh"
$web = google-chrome-stable
$media = vlc
$logout = wlogout
$scriptsDir = "$HOME/.config/hypr/scripts"
$screenshot = flameshot 

#################
### AUTOSTART ###
#################

exec-once = "$HOME/.config/hypr/scripts/startup"


#############################
### ENVIRONMENT IABLES ###
#############################
env = HYPRCURSOR_THEME,Bibata-Modern-Ice
env = HYPRCURSOR_SIZE,24
env = XCURSOR_THEME,Bibata-Modern-Ice
env = XCURSOR_SIZE,24
env = TZ, America/Sao_Paulo

# XDG Desktop Portal
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

# Configurações para aplicativos Qt
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1

#corrigi a integração do dolphin
env = XDG_MENU_PREFIX,arch-

# GDK
env = GDK_SCALE,1

# Toolkit Backend
env = GDK_BACKEND,wayland,x11,*
env = CLUTTER_BACKEND,wayland

# Mozilla
env = MOZ_ENABLE_WAYLAND,1

#Desabilitar o iniciador do appimage por padrão
env = APPIMAGELAUNCHER_DISABLE,1

# Ozone
env = OZONE_PLATFORM,wayland
env = ELECTRON_OZONE_PLATFORM_HINT,wayland

#env = QT_QPA_PLATFORMTHEME,qt5ct  # ou qt6ct
#env = QT_STYLE_OVERRIDE,adwaita  # ou outro tema se necessário

env = QT_QPA_PLATFORM=wayland

env = XDG_CURRENT_DESKTOP=sway

#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 3
    gaps_out = 7

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
   # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
     col.active_border = rgb(00008B) rgb(6495ED) 45deg #cores da borda
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}


 animations {
  enabled = true

  # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

  bezier = myBezier, 0.05, 0.9, 0.1, 1.05

  animation = windows, 1, 6, myBezier
  animation = windowsOut, 1, 4, default, popin 80%
  animation = border, 1, 6, default
  animation = fade, 1, 4, default
  animation = workspaces, 1, 4, default
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

#kitty abre em janela solta e centralizado
   windowrulev2 = float, class:^(kitty)$, title:^(kitty)$
   windowrulev2 = size 1100 600, class:^(kitty)$, title:^(kitty)$
   windowrulev2 = move 480 200, class:^(kitty)$, title:^(kitty)$

# Pavucontrol floating
windowrulev2 = float,class:(.*org.pulseaudio.pavucontrol.*)
windowrulev2 = size 700 600,class:(.*org.pulseaudio.pavucontrol.*)
windowrulev2 = center,class:(.*org.pulseaudio.pavucontrol.*)
#windowrulev2 = pin,class:(.*org.pulseaudio.pavucontrol.*)

# Gnome Calculator
windowrulev2 = float,class:(org.gnome.Calculator)
windowrulev2 = size 450 700,class:(org.gnome.Calculator)
windowrulev2 = center,class:(org.gnome.Calculator)

# Gnome Calendario
windowrulev2 = float,class:(org.gnome.Calendar)
windowrulev2 = size 550 100,class:(org.gnome.Calendar)
windowrulev2 = center,class:(org.gnome.Calendar)

# Gnome gnome weather
windowrulev2 = float,class:(org.gnome.Weather)
windowrulev2 = size 550 100,class:(org.gnome.Calendar)
windowrulev2 = center,class:(org.gnome.weather)

# Obs-studio floating
windowrulev2 = float,class:(com.obsproject.Studio)
windowrulev2 = size 1300 1000,class:(com.obsproject.Studio)
windowrulev2 = move 01% 06%,class:(com.obsproject.Studio)
#windowrulev2 = pin, class:(steam)


# gnome-disks
windowrulev2 = float,class:(.*gnome.Disk.*)
windowrulev2 = size 900 600,class:(.*gnome-disks.*)
windowrulev2 = center,class:(.*gnome-disks.*)
#windowrulev2 = pin,class:(.*gnome-disks.*)


# steam floating
#windowrulev2 = float,class:(steam)
#windowrulev2 = size 1200 1000,class:(steam)
#windowrulev2 = move 01% 06%,class:(steam)

# vlc floating
windowrulev2 = float,class:(vlc)
windowrulev2 = size 800 1000,class:(vlc)
windowrulev2 = move 01% 06%,class:(vlc)

#clipse
windowrulev2 = float,class:(clipse) # abrir em janela flutuante
windowrulev2 = size 580 580,class:(clipse) # defina o tamanho da janela conforme necessário

# pamac
windowrulev2 = float,class:(.*pamac.manager.*)
windowrulev2 = size 1200 1000,class:(.*pamac.manager.*)
windowrulev2 = move 01% 06%,class:(pamac-manager)

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant = intl
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, F, fullscreen
bind = $mainMod SHIFT, F, fullscreen, 1
bind = $mainMod, M, exit,
bind = $mainMod, F1, exec, firefox
bind = $mainMod, F2, exec, $web
bind = $mainMod, F4, exec, gimp
bind = $mainMod, F6, exec, $media
bind = $mainMod, F8, exec, $fileManager
bind = $mainMod, F9, exec, pavucontrol
bind = $mainMod, A, exec, alacritty 
bind = $mainMod, V, togglefloating,
bind = $mainMod, F11, exec, rofi -theme launcher2.rasi -show drun -show-icons -lines 6 -columns 4
bind = $mainMod, F12, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, X, exec, $logout 
bind = $mainMod, C, exec, kitty --class clipse -e 'clipse' # inicia o clipse
bind = ALT, F, exec, ~/.config/hypr/scripts/changewallpaper
bind = $mainMod CTRL, PRINT, exec, grim -g "$(slurp)" ~/Imagens/screenshot_$(date +'%Y-%m-%d_%H-%M-%S').png
bind = $mainMod, PRINT, exec, $screenshot 
bind = $mainMod, SPACE, exec, pkill waybar || waybar #fecha e abre a waybar
bind = $mainMod, B, exec, pkill waybar || ~/.config/hypr/scripts/statusbar 

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(alacritty)$,title:^(alacritty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
